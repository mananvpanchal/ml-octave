function theta = gradientDescent(X, y, theta, alpha, num_iters)

m = length(y);

for iter = 1:num_iters

    % ============================================================
    
    % err = zeros(1, m);
    
    % del = zeros(1, length(theta));
    
    % ------------------------------------------
    % for mi = 1:m
    %    err(mi) = theta' * X(mi, :)' - y(mi, 1);
    % end
    % ----------------- or --------------------------
        
    % err = (theta' * X') - y';
    
    % -----------------------------------------------
    
    % -------------------------
    % for ni = 1:length(theta)
    %    del(ni) = err * X(:, ni);
    % end
    % -------- or --------------
    
    % del = err * X;
    
    % -----------------------------------------------
            
    % theta = theta - (alpha / m) * del';
    
    % =================== or =====================================
    
    theta = theta - (alpha / m) * (((theta' * X') - y') * X)'

    % ============================================================

end
